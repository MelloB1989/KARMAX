/karmax
# ...and any local backup of it. /karmax is anchored, so it matched the binary
# but not karmax.pre-wasm-backup, which is how a 50MB blob reached the remote.
# Backups belong in ~/.karmax, not here.
/karmax.pre-*
/karmax.*-backup
/nexus
.env
*.log
*.service.log
*.lock
*.success
.scheduled-*
.claude-karmax-*
.claude/
PLAN.md
karmax.yaml

# Built WASM guest for the extension-tier spike (reproduce with the command in
# spikes/wasm/FINDINGS.md).
spikes/wasm/guest.wasm

# Built and signed loop artifacts (make loops).
dist/
/memingest
/memverify
/memread
/memcat
